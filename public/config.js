// CONFIG.js - Configuration file for PropTrack
// ⚠️ IMPORTANT: Update these values before deployment

// 1. AUTO-DETECT API URL OR SET MANUALLY
// If frontend and backend are on the same domain, use relative path
// Otherwise, specify the full backend URL
const API_BASE_URL = window.location.origin; // Auto-detect current domain

// For separate backend, uncomment and update:
// const API_BASE_URL = 'https://proptrack-rho.vercel.app';

// 2. ENVIRONMENT VARIABLES TO SET IN VERCEL
// Go to Vercel Dashboard > Your Project > Settings > Environment Variables
// Add the following:

/*
REQUIRED:
- CRON_SECRET: A random string for cron job authentication (generate a random 32+ character string)
- ODDS_API_KEY: Your API key from https://the-odds-api.com (sign up for free tier)
- VERCEL_BLOB_READ_WRITE_TOKEN: Auto-generated by Vercel Blob Storage (no action needed)

OPTIONAL (for advanced scheduling):
- QSTASH_TOKEN: From https://upstash.com/qstash (for dynamic pre-game updates)
*/

// 3. VERCEL BLOB STORAGE
// Blob storage is automatically provisioned when you:
// - Install @vercel/blob package
// - Deploy to Vercel
// - Vercel will auto-generate the VERCEL_BLOB_READ_WRITE_TOKEN

// 4. ODDS API SETUP
// Sign up at: https://the-odds-api.com
// Free tier includes: 500 requests/month
// Get your API key and add it to Vercel environment variables

// 5. DEPLOYMENT CHECKLIST
// □ Update API_BASE_URL above
// □ Set CRON_SECRET in Vercel
// □ Set ODDS_API_KEY in Vercel
// □ Verify Blob Storage is enabled
// □ Deploy and test!

// Export for use in HTML
if (typeof module !== 'undefined' && module.exports) {
  module.exports = { API_BASE_URL };
}
